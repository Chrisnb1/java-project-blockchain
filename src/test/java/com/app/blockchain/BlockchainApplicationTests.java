package com.app.blockchain;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BlockchainApplicationTests {

	@Test
	void contextLoads() {
		/*
		List<Block> cadena1 = new ArrayList<>();
		List<Block> cadena2 = new ArrayList<>();

		INode n1 = new Node(cadena1);
		INode n2 = new Node(cadena2);

		Block b1 = new Block(n1.getLastBlock().getIndex() + 1, "data1block", n1.getLastBlock().getHash());
		n1.addBlock(b1);
		Block b2 = new Block(n1.getLastBlock().getIndex() + 1, "data2block", n1.getLastBlock().getHash());
		n1.addBlock(b2);
		Block b3 = new Block(n1.getLastBlock().getIndex() + 1, "data3block", n1.getLastBlock().getHash());
		n1.addBlock(b3);

		Block b1_1 = new Block(n2.getLastBlock().getIndex() + 1, "data1_1block", n2.getLastBlock().getHash());
		n2.addBlock(b1_1);
		Block b2_1 = new Block(n2.getLastBlock().getIndex() + 1, "data2_1block", n2.getLastBlock().getHash());
		n2.addBlock(b2_1);

        System.out.println(n2 + "\n");
        n2.longestChain(cadena1);
        System.out.println(n2 + "\n");*/
	}

}
